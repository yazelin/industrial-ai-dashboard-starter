[project]
name = "industrial-ai-dashboard-starter"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "fastapi==0.115.6",
    "uvicorn[standard]==0.34.0",
]

[project.optional-dependencies]
mqtt = ["amqtt>=0.11,<0.12"]
ai = ["httpx>=0.28,<0.29"]

[tool.uv]
package = false
